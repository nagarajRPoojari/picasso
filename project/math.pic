import syncio from builtin;
import utils;

class Adder {
    say u: Utils;
    fn Adder() {
        this.u = new Utils();
    }

    fn add(a: int, b: int): int {
        syncio.printf(" this is in math.Adder.add \n");
        return a + b + this.u.giveOne();
    }
}