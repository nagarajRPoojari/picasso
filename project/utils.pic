import syncio from builtin;
import dummy;

class Utils {
    fn Utils() {
    }

    fn giveOne(): int {
        say x: Dummy = new Dummy();

        syncio.printf(" this is in utils.Utils.giveOne \n");  

        return 1 + x.giveTwo();
    }
}